/**
 * Type definitions for Notification components
 */

import type {HasColorVariant, HasTag} from '@/components';
import type {ButtonSizes, ColorVariants} from '@/index';

/**
 * Props interface for the Popover component.
 */
export interface PopoverProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Popover
}

/**
 * Slot definitions for Popover component
 */
export interface PopoverSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Popover component
 */
export interface PopoverEmits {
  // TODO: Add specific emits for Popover
}

/**
 * Props interface for the Toast component.
 */
export interface ToastProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Toast
}

/**
 * Slot definitions for Toast component
 */
export interface ToastSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Toast component
 */
export interface ToastEmits {
  // TODO: Add specific emits for Toast
}

/**
 * Props interface for the ToastBody component.
 */
export interface ToastBodyProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for ToastBody
}

/**
 * Slot definitions for ToastBody component
 */
export interface ToastBodySlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for ToastBody component
 */
export interface ToastBodyEmits {
  // TODO: Add specific emits for ToastBody
}

/**
 * Props interface for the ToastContainer component.
 */
export interface ToastContainerProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for ToastContainer
}

/**
 * Slot definitions for ToastContainer component
 */
export interface ToastContainerSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for ToastContainer component
 */
export interface ToastContainerEmits {
  // TODO: Add specific emits for ToastContainer
}

/**
 * Props interface for the Tooltip component.
 */
export interface TooltipProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Tooltip
}

/**
 * Slot definitions for Tooltip component
 */
export interface TooltipSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Tooltip component
 */
export interface TooltipEmits {
  // TODO: Add specific emits for Tooltip
}