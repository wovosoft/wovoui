/**
 * Type definitions for PageLayout components
 */

import type {HasColorVariant, HasTag} from '@/components';
import type {ButtonSizes, ColorVariants} from '@/index';

/**
 * Props interface for the PageAside component.
 */
export interface PageAsideProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for PageAside
}

/**
 * Slot definitions for PageAside component
 */
export interface PageAsideSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for PageAside component
 */
export interface PageAsideEmits {
  // TODO: Add specific emits for PageAside
}

/**
 * Props interface for the PageContainer component.
 */
export interface PageContainerProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for PageContainer
}

/**
 * Slot definitions for PageContainer component
 */
export interface PageContainerSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for PageContainer component
 */
export interface PageContainerEmits {
  // TODO: Add specific emits for PageContainer
}

/**
 * Props interface for the PageFooter component.
 */
export interface PageFooterProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for PageFooter
}

/**
 * Slot definitions for PageFooter component
 */
export interface PageFooterSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for PageFooter component
 */
export interface PageFooterEmits {
  // TODO: Add specific emits for PageFooter
}

/**
 * Props interface for the PageHeader component.
 */
export interface PageHeaderProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for PageHeader
}

/**
 * Slot definitions for PageHeader component
 */
export interface PageHeaderSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for PageHeader component
 */
export interface PageHeaderEmits {
  // TODO: Add specific emits for PageHeader
}

/**
 * Props interface for the PageMain component.
 */
export interface PageMainProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for PageMain
}

/**
 * Slot definitions for PageMain component
 */
export interface PageMainSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for PageMain component
 */
export interface PageMainEmits {
  // TODO: Add specific emits for PageMain
}