/**
 * Type definitions for Native components
 */

import type {HasColorVariant, HasTag} from '@/components';
import type {ButtonSizes, ColorVariants} from '@/index';

/**
 * Props interface for the NativeCollapse component.
 */
export interface NativeCollapseProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for NativeCollapse
}

/**
 * Slot definitions for NativeCollapse component
 */
export interface NativeCollapseSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for NativeCollapse component
 */
export interface NativeCollapseEmits {
  // TODO: Add specific emits for NativeCollapse
}

/**
 * Props interface for the NativeDialog component.
 */
export interface NativeDialogProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for NativeDialog
}

/**
 * Slot definitions for NativeDialog component
 */
export interface NativeDialogSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for NativeDialog component
 */
export interface NativeDialogEmits {
  // TODO: Add specific emits for NativeDialog
}

/**
 * Props interface for the NativeProgress component.
 */
export interface NativeProgressProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for NativeProgress
}

/**
 * Slot definitions for NativeProgress component
 */
export interface NativeProgressSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for NativeProgress component
 */
export interface NativeProgressEmits {
  // TODO: Add specific emits for NativeProgress
}