/**
 * Type definitions for Modal components
 */

import type {HasColorVariant, HasTag} from '@/components';
import type {ButtonSizes, ColorVariants} from '@/index';

/**
 * Props interface for the Modal component.
 */
export interface ModalProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Modal
}

/**
 * Slot definitions for Modal component
 */
export interface ModalSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Modal component
 */
export interface ModalEmits {
  // TODO: Add specific emits for Modal
}

/**
 * Props interface for the ModalBody component.
 */
export interface ModalBodyProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for ModalBody
}

/**
 * Slot definitions for ModalBody component
 */
export interface ModalBodySlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for ModalBody component
 */
export interface ModalBodyEmits {
  // TODO: Add specific emits for ModalBody
}

/**
 * Props interface for the ModalDialog component.
 */
export interface ModalDialogProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for ModalDialog
}

/**
 * Slot definitions for ModalDialog component
 */
export interface ModalDialogSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for ModalDialog component
 */
export interface ModalDialogEmits {
  // TODO: Add specific emits for ModalDialog
}

/**
 * Props interface for the ModalFooter component.
 */
export interface ModalFooterProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for ModalFooter
}

/**
 * Slot definitions for ModalFooter component
 */
export interface ModalFooterSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for ModalFooter component
 */
export interface ModalFooterEmits {
  // TODO: Add specific emits for ModalFooter
}

/**
 * Props interface for the ModalHeader component.
 */
export interface ModalHeaderProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for ModalHeader
}

/**
 * Slot definitions for ModalHeader component
 */
export interface ModalHeaderSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for ModalHeader component
 */
export interface ModalHeaderEmits {
  // TODO: Add specific emits for ModalHeader
}

/**
 * Props interface for the ModalTitle component.
 */
export interface ModalTitleProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for ModalTitle
}

/**
 * Slot definitions for ModalTitle component
 */
export interface ModalTitleSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for ModalTitle component
 */
export interface ModalTitleEmits {
  // TODO: Add specific emits for ModalTitle
}