/**
 * Type definitions for Layout components
 */

import type {HasColorVariant, HasTag} from '@/components';
import type {ButtonSizes, ColorVariants} from '@/index';

/**
 * Props interface for the Col component.
 */
export interface ColProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Col
}

/**
 * Slot definitions for Col component
 */
export interface ColSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Col component
 */
export interface ColEmits {
  // TODO: Add specific emits for Col
}

/**
 * Props interface for the Column component.
 */
export interface ColumnProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Column
}

/**
 * Slot definitions for Column component
 */
export interface ColumnSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Column component
 */
export interface ColumnEmits {
  // TODO: Add specific emits for Column
}

/**
 * Props interface for the Container component.
 */
export interface ContainerProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Container
}

/**
 * Slot definitions for Container component
 */
export interface ContainerSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Container component
 */
export interface ContainerEmits {
  // TODO: Add specific emits for Container
}

/**
 * Props interface for the Flex component.
 */
export interface FlexProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Flex
}

/**
 * Slot definitions for Flex component
 */
export interface FlexSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Flex component
 */
export interface FlexEmits {
  // TODO: Add specific emits for Flex
}

/**
 * Props interface for the FlexItem component.
 */
export interface FlexItemProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for FlexItem
}

/**
 * Slot definitions for FlexItem component
 */
export interface FlexItemSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for FlexItem component
 */
export interface FlexItemEmits {
  // TODO: Add specific emits for FlexItem
}

/**
 * Props interface for the Layout component.
 */
export interface LayoutProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Layout
}

/**
 * Slot definitions for Layout component
 */
export interface LayoutSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Layout component
 */
export interface LayoutEmits {
  // TODO: Add specific emits for Layout
}

/**
 * Props interface for the Row component.
 */
export interface RowProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Row
}

/**
 * Slot definitions for Row component
 */
export interface RowSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Row component
 */
export interface RowEmits {
  // TODO: Add specific emits for Row
}