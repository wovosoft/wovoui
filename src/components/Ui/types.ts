/**
 * Type definitions for Ui components
 */

import type {HasColorVariant, HasTag} from '@/components';
import type {ButtonSizes, ColorVariants} from '@/index';

/**
 * Props interface for the Aspect component.
 */
export interface AspectProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Aspect
}

/**
 * Slot definitions for Aspect component
 */
export interface AspectSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Aspect component
 */
export interface AspectEmits {
  // TODO: Add specific emits for Aspect
}

/**
 * Props interface for the Badge component.
 */
export interface BadgeProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Badge
}

/**
 * Slot definitions for Badge component
 */
export interface BadgeSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Badge component
 */
export interface BadgeEmits {
  // TODO: Add specific emits for Badge
}

/**
 * Props interface for the Chart component.
 */
export interface ChartProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Chart
}

/**
 * Slot definitions for Chart component
 */
export interface ChartSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Chart component
 */
export interface ChartEmits {
  // TODO: Add specific emits for Chart
}

/**
 * Props interface for the Figure component.
 */
export interface FigureProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Figure
}

/**
 * Slot definitions for Figure component
 */
export interface FigureSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Figure component
 */
export interface FigureEmits {
  // TODO: Add specific emits for Figure
}

/**
 * Props interface for the Icon component.
 */
export interface IconProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Icon
}

/**
 * Slot definitions for Icon component
 */
export interface IconSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Icon component
 */
export interface IconEmits {
  // TODO: Add specific emits for Icon
}

/**
 * Props interface for the Placeholder component.
 */
export interface PlaceholderProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Placeholder
}

/**
 * Slot definitions for Placeholder component
 */
export interface PlaceholderSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Placeholder component
 */
export interface PlaceholderEmits {
  // TODO: Add specific emits for Placeholder
}

/**
 * Props interface for the Vr component.
 */
export interface VrProps extends HasTag, HasColorVariant {
  // TODO: Add specific props for Vr
}

/**
 * Slot definitions for Vr component
 */
export interface VrSlots {
  /**
   * Default slot
   */
  default(props: {}): any;
}

/**
 * Emit definitions for Vr component
 */
export interface VrEmits {
  // TODO: Add specific emits for Vr
}